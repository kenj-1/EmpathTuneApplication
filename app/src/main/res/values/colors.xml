<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ── Backgrounds ── -->
    <color name="dashboard_bg">#FFFFFF</color>
    <color name="profile_bg">#FFFFFF</color>

    <!-- ── Yellow accent (#f2fb41 @ 75% → #BFF2FB41) ── -->
    <color name="yellow_accent">#BFF2FB41</color>
    <color name="yellow_accent_solid">#F2FB41</color>

    <!-- ── Bottom nav (#e9ee67) ── -->
    <color name="yellow_nav">#E9EE67</color>

    <!-- ── Gradient board (#edda0a → #bbbbbb) ── -->
    <color name="gradient_start">#EDDA0A</color>
    <color name="gradient_end">#BBBBBB</color>

    <!-- ── Profile board multi-stop gradient colours ── -->
    <color name="profile_gradient_start">#BBBBBB</color>
    <color name="profile_gradient_mid">#EDDA0A</color>
    <color name="profile_gradient_end">#BBBBBB</color>

    <!-- ── Input / field backgrounds ── -->
    <color name="input_bg">#D9D9D9</color>
    <color name="field_bg">#D9D9D9</color>

    <!-- ── Overlays ── -->
    <color name="blur_overlay">#80070504</color>            <!-- 50% dark -->
    <color name="shadow_color">#33000000</color>            <!-- 20% black shadow -->
    <color name="menu_bg_color">#FFFFFFFF</color>

    <!-- ── Text ── -->
    <color name="text_black">#000000</color>
    <color name="text_hint">#66000000</color>
    <color name="text_white">#FFFFFF</color>
    <color name="label_text">#000000</color>

    <!-- ── Inner circles ── -->
    <color name="white_94">#F0FFFFFF</color>

    <!-- ── SeekBar track (white @ 80%) ── -->
    <color name="seekbar_track">#CCffffff</color>
</resources>
